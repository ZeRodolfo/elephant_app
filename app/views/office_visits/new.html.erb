<% content_for(:html_title) { 'Elephant - Gerenciamento Para Psicologia Clínica | Nova Consulta' } %>

<%= render 'shared/navbar' %>

<style>
  main {
    height: 100%;
  }
</style>

<section class="el min-100 d-flex flex-column">
	<% if params[:register].present? %>
		<%= render 'register', multiple: true, office_visit: @office_visit, title: 'Nova Consulta' %>
	<% else %>
		<%= render 'form', office_visit: @office_visit %>
	<% end %>
</section>