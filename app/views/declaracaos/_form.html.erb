<%= form_with(model: declaracao, local: true) do |form| %>
  <% if declaracao.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(declaracao.errors.count, "error") %> prohibited this declaracao from being saved:</h2>

      <ul>
      <% declaracao.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
