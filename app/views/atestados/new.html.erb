<% content_for(:html_title) { 'Elephant - Gerenciamento Para Psicologia Clínica | Novo atestado' } %>

<%= render 'shared/navbar' %>

<section class="d-flex m-0 align-items-start justify-content-center el min100">
  <div class="bounding generic-form">
    <h1 class="bold">Novo Atestado</h1>
    <%= render 'form', atestado: @atestado, path: patient_atestados_path, method: :post %>
  </div>
</section>
