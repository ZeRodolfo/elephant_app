<% content_for(:html_title) { 'Elephant - Gerenciamento Para Psicologia Clínica | Novo relatório' } %>

<%= render 'shared/navbar' %>

<section class="d-flex m-0 align-items-start justify-content-center el min100">
  <div class="bounding ojiasasdop">
    <h1 class="bold">Novo Relatório</h1>
    <%= render 'form', relatorio: @relatorio, path: patient_relatorios_path, method: :post %>
  </div>
</section>
