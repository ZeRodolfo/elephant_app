<% content_for(:html_title) { 'Elephant - Gerenciamento Para Psicologia Clínica | Novo parecer' } %>

<%= render 'shared/navbar' %>

<section class="d-flex m-0 align-items-start justify-content-center el min100">
  <div class="bounding ojiasasdop">
    <h1 class="bold">Novo Parecer</h1>
    <%= render 'form', parecer: @parecer, path: patient_pareceres_path, method: :post %>
  </div>
</section>
