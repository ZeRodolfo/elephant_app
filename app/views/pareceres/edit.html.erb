<% content_for(:html_title) { 'Elephant - Gerenciamento Para Psicologia Clínica | Editar parecer' } %>

<%= render 'shared/navbar' %>

<section class="d-flex m-0 align-items-start justify-content-center el min100">
  <div class="bounding ojiasasdop">
    <h1 class="bold">Editar Parecer</h1>
    <%= render 'form', parecer: @parecer, path: patient_parecer_path, method: :patch  %>
  </div>
</section>